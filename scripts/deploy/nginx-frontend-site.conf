server {
    server_name argroupofeducation.com www.argroupofeducation.com;

    location /api/wp-media/ {
        rewrite ^/api/wp-media/(.*)$ /wp-content/$1 break;
        proxy_pass http://nextjs_backend;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        proxy_pass http://nextjs_backend;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 20M;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/argroupofeducation.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/argroupofeducation.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    if ($host = www.argroupofeducation.com) {
        return 301 https://$host$request_uri;
    }

    if ($host = argroupofeducation.com) {
        return 301 https://$host$request_uri;
    }

    listen 80;
    server_name argroupofeducation.com www.argroupofeducation.com;
    return 404;
}
