# Legacy Postgres / CockroachSQL migrations (pre–Hostinger MySQL cutover).
# Marketing DB on Hostinger uses `prisma db push` against schema.prisma (provider = mysql).
# Do not run `prisma migrate deploy` against Hostinger MySQL with these SQL files.
