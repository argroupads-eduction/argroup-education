# apps/strapi-src

Source for Strapi content-types / lifecycles (committed in the monorepo).

**Runtime app (not on C: OneDrive — disk space):** `D:\ar-group-strapi`

Copy after pulling:

```bat
robocopy apps\strapi-src\src D:\ar-group-strapi\src /E
```

See `docs/STRAPI_STEP3.md`.
